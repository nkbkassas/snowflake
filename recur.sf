snowflake fib(n):
    form
    if(lte(n, 2)):
        form
        1;
        freeze
     else:
        form
        add(fib( subtract(n, 1)), fib(subtract(n,2)));
        freeze
    freeze
crystalize(fib(10));
